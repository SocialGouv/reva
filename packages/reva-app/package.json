{
  "name": "reva-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "concurrently npm:dev:*",
    "dev:elm": "parcel index.html --port 1235 --public-url /app",
    "dev:css": "npm run css -- --watch",
    "build": "npm run build:css && npm run build:elm && cp -R ./dist ../reva-api/dist/app",
    "build:css": "NODE_ENV=production npm run css -- --minify",
    "build:elm": "parcel build index.html --public-url /app",
    "css": "npx tailwindcss -i ./css/style.css -o ./style.css",
    "postinstall": "npm run css"
  },
  "keywords": [],
  "dependencies": {
  },
  "devDependencies": {
    "@parcel/transformer-elm": "^2.2.1",
    "@parcel/transformer-typescript-tsc": "^2.2.1",
    "concurrently": "^7.0.0",
    "parcel": "^2.2.1",
    "tailwindcss": "^3.0.18"
  }
}
