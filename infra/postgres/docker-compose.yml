version: "3.1"

services:
  dbreva:
    image: postgres
    container_name: postgresql_reva
    restart: always
    environment:
      POSTGRES_DB: reva
      POSTGRES_USER: reva
      POSTGRES_PASSWORD: password
    ports:
      - 5444:5432
